
<% if (page.prev) { %>
    <a class="nav-btn" href="<%- url_for(config.root + page.prev.path) %>" title="<%-page.prev.title %>">&larr; Prev</a>
<% } %>

<% if (page.next) { %>
    <a class="nav-btn" href="<%- url_for(config.root + page.next.path) %>" title="<%-page.next.title %>">Next &rarr;</a>
<% } %>
